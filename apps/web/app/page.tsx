export default function FoundationPage() {
  return (
    <main>
      <p className="eyebrow">f(AI) Studio</p>
      <h1>Control Plane Foundation</h1>
      <p>
        The web, worker, PostgreSQL, migrations and health checks are ready for
        the first governed delivery slice.
      </p>
    </main>
  );
}
