_config_version: 34

model:
  provider: openai-codex
  default: gpt-5.6-terra

terminal:
  backend: local
  cwd: /opt/data/work/project

platform_toolsets:
  api_server:
    - terminal
    - fai_internal
    - no_mcp

agent:
  max_turns: 4

plugins:
  enabled:
    - fai-control-plane

toolsets:
  - fai_internal
  - clarify
